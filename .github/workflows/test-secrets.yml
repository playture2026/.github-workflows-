name: Test Secrets

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */3 * * *'  # 每3小时执行一次

env:
  # 将Secrets设置为环境变量
  DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
  PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
  EMAIL_USERNAME: ${{ secrets.EMAIL_USERNAME }}
  EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
  EMAIL_TO: ${{ secrets.EMAIL_TO }}
  LIBLIB_ACCESS_TOKEN: ${{ secrets.LIBLIB_ACCESS_TOKEN }}

jobs:
  test-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: 获取代码
        uses: actions/checkout@v4
      
      - name: 设置Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 测试所有Secrets
        run: |
          python << 'EOF'
          import os
          from datetime import datetime
          
          print("=" * 80)
          print("🔍 GitHub Actions Secrets配置验证")
          print("=" * 80)
          print()
          
          secrets = {
              "DEEPSEEK_API_KEY": {
                  "value": os.environ.get('DEEPSEEK_API_KEY', ''),
                  "必需": True,
                  "说明": "AI生成文章的核心API密钥"
              },
              "PAT_TOKEN": {
                  "value": os.environ.get('PAT_TOKEN', ''),
                  "必需": True,
                  "说明": "用于Git推送代码到仓库"
              },
              "EMAIL_USERNAME": {
                  "value": os.environ.get('EMAIL_USERNAME', ''),
                  "必需": True,
                  "说明": "发送邮件的邮箱地址"
              },
              "EMAIL_PASSWORD": {
                  "value": os.environ.get('EMAIL_PASSWORD', ''),
                  "必需": True,
                  "说明": "邮箱授权码"
              },
              "EMAIL_TO": {
                  "value": os.environ.get('EMAIL_TO', ''),
                  "必需": True,
                  "说明": "接收邮件的邮箱地址"
              },
              "LIBLIB_ACCESS_TOKEN": {
                  "value": os.environ.get('LIBLIB_ACCESS_TOKEN', ''),
                  "必需": False,
                  "说明": "AI生成配图的访问令牌（可选）"
              }
          }
          
          valid_count = 0
          total_required = sum(1 for s in secrets.values() if s["必需"])
          
          print("📋 Secrets配置状态：")
          print("-" * 80)
          
          for secret_name, config in secrets.items():
              value = config["value"]
              is_required = config["必需"]
              description = config["说明"]
              
              if value:
                  if is_required:
                      status = "✅ 已配置"
                      valid_count += 1
                  else:
                      status = "✅ 已配置（可选）"
                  display_value = f"✓ (长度: {len(value)} 字符)"
              else:
                  if is_required:
                      status = "❌ 未配置"
                  else:
                      status = "⚠️  未配置（可选）"
                  display_value = "✗ (未配置)"
              
              print(f"\n{status}")
              print(f"Secret名称: {secret_name}")
              print(f"说明: {description}")
              print(f"值: {display_value}")
          
          print()
          print("=" * 80)
          print("📊 验证结果:")
          print("=" * 80)
          
          print(f"必需Secrets: {valid_count}/{total_required} 已配置")
          
          if valid_count == total_required:
              print("✅ 所有必需Secrets已正确配置！")
              print()
              print("📋 建议的下一步:")
              print("1. 运行主工作流 'Hot Topic Auto Writer with Images (LiblibAI)'")
              print("2. 查看运行日志，确认文章生成成功")
              print("3. 检查邮件内容")
          else:
              missing_count = total_required - valid_count
              print(f"❌ 还有 {missing_count} 个必需Secrets未配置")
              print()
              print("📋 请配置以下缺失的Secrets:")
              for secret_name, config in secrets.items():
                  if config["必需"] and not config["value"]:
                      print(f"  - {secret_name}: {config['说明']}")
          
          print()
          print("=" * 80)
          print(f"验证完成时间: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
          print("=" * 80)
          EOF
      
      - name: 测试DeepSeek API连接
        run: |
          if [ -z "$DEEPSEEK_API_KEY" ]; then
            echo "⚠️  DEEPSEEK_API_KEY未配置，跳过API连接测试"
            exit 0
          fi
          
          python << 'EOF'
          import os
          from openai import OpenAI
          
          print("=" * 80)
          print("🔍 测试DeepSeek API连接")
          print("=" * 80)
          print()
          
          api_key = os.environ.get('DEEPSEEK_API_KEY', '')
          
          if not api_key:
              print("❌ DEEPSEEK_API_KEY未配置")
              exit(1)
          
          print(f"✓ DEEPSEEK_API_KEY已配置 (长度: {len(api_key)} 字符)")
          print()
          print("正在测试API连接...")
          
          try:
              client = OpenAI(
                  api_key=api_key,
                  base_url="https://api.deepseek.com"
              )
              
              # 发送一个简单的测试请求
              response = client.chat.completions.create(
                  model="deepseek-chat",
                  messages=[
                      {"role": "user", "content": "请回复'测试成功'"}
                  ],
                  max_tokens=10
              )
              
              result = response.choices[0].message.content
              print(f"✅ API连接成功！")
              print(f"   响应: {result}")
              print()
              print("📋 DeepSeek API配置正确，可以正常使用")
              
          except Exception as e:
              print(f"❌ API连接失败: {str(e)}")
              print()
              print("📋 可能的原因:")
              print("  1. API密钥无效或已过期")
              print("  2. API服务暂时不可用")
              print("  3. 网络连接问题")
              exit(1)
          
          print("=" * 80)
          EOF
      
      - name: 测试邮件发送
        run: |
          if [ -z "$EMAIL_USERNAME" ] || [ -z "$EMAIL_PASSWORD" ] || [ -z "$EMAIL_TO" ]; then
            echo "⚠️  邮件相关Secrets未配置，跳过邮件发送测试"
            exit 0
          fi
          
          echo "✓ 邮件Secrets已配置"
          echo "📧 正在发送测试邮件..."
      
      - name: 测试Git推送
        run: |
          if [ -z "$PAT_TOKEN" ]; then
            echo "⚠️  PAT_TOKEN未配置，跳过Git推送测试"
            exit 0
          fi
          
          echo "测试Git推送权限..."
          
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          
          # 使用PAT_TOKEN进行认证
          git remote set-url origin https://x-access-token:${PAT_TOKEN}@github.com/${GITHUB_REPOSITORY}.git
          
          # 测试获取远程分支
          git fetch origin main
          
          if [ $? -eq 0 ]; then
              echo "✅ Git推送权限测试成功！"
              echo "   PAT_TOKEN配置正确，可以正常推送代码"
          else
              echo "❌ Git推送权限测试失败"
              echo "   PAT_TOKEN可能无效或权限不足"
              exit 1
          fi
